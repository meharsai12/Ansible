- name: Install nginx
  hosts: frontend
  become: yes
  tasks:
  - name: installing nginx
    ansible.builtin.module:
      name: nginx
      state: present
  
  - name: Start nginx
    ansible.builtin.service:
      name: nginx
      enabled: yes
      
